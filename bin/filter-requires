#!/bin/bash

# Usage:
# %define _use_internal_dependency_generator 0
# %define __find_requires %{iov_prefix} bin/filter-requires Mac\\|VMS\\|perl >=

`rpm --eval "%__find_requires"` | sed "s/perl(/$1(/g" | grep -v -E "$1"

